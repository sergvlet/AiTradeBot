# ARCHITECTURE_V4 — план внедрения (чеклист с галочками)

Этот документ — пошаговый чеклист для внедрения архитектуры v4 в AiTradeBot.  
Отмечай выполненные пункты галочками `- [x]`.

---

## 🎯 Глобальная цель

Привести проект к архитектуре, в которой:

- Стратегии изолированы, плагинные и работают через единый интерфейс.
- Запуск/остановка/мониторинг стратегий централизованы через Orchestrator.
- WEB-слой ничего не знает о деталях стратегий и бирж.
- Market/Exchange-слой скрыт за абстракциями.
- Код очищен от дублей, legacy и мусора.
- Frontend разделён на аккуратные JS-модули.

---

# ✅ 1. Подготовка и анализ проекта

- [ ] **1.1.** Запустить полное сканирование проекта в IntelliJ IDEA  
  *(Analyze → Inspect Code → Whole project)*
- [ ] **1.2.** Сохранить отчёт по предупреждениям и ошибкам (Inspection Results)
- [ ] **1.3.** Сохранить отдельный список *неиспользуемых методов* (Unused declarations)
- [ ] **1.4.** Сохранить отдельный список *неиспользуемых классов*
- [ ] **1.5.** Отдельно выписать найденные *дубликаты кода* (Duplicate code / Duplicated blocks)

---

# ✅ 2. Чистка структуры (safe cleanup)

- [ ] **2.1.** Удалить дубликат интерфейса  
  `com.chicu.aitradebot.service.ExchangeSettingsService`  
  (оставить только `com.chicu.aitradebot.exchange.service.ExchangeSettingsService`)
- [ ] **2.2.** Проверить наличие старых версий SmartFusion (v1/v2) и:
    - [ ] отметить их `@Deprecated`, если ещё используются в единичных местах
    - [ ] либо удалить, если реально не используются
- [ ] **2.3.** Пометить аннотацией `@Deprecated` кандидатов на удаление:
    - [ ] `CandleResampler`
    - [ ] `TradeCacheService`
    - [ ] `CandleServiceAdapter`
    - [ ] `TimeUtil`
    - [ ] `Kline`
    - [ ] `BalanceInfo`
- [ ] **2.4.** Создать список «Классы, которые можно удалить после стабилизации v4»
- [ ] **2.5.** Создать файл `ARCHITECTURE_V4.md` (этот чеклист) в корне проекта и закоммитить

---

# ✅ 3. Нормализация стратегий

- [ ] **3.1.** Уточнить интерфейс `TradingStrategy v4` с методами:
    - [ ] `void start(StrategyContext ctx);`
    - [ ] `void stop();`
    - [ ] `void onPriceUpdate(Candle candle);`
    - [ ] `boolean isActive();`
    - [ ] `StrategyRunInfo getRunInfo();`
- [ ] **3.2.** Привести все стратегии к этому интерфейсу:
    - [ ] `SmartFusionStrategy`
    - [ ] `ScalpingStrategy`
    - [ ] `RsiEmaStrategy`
    - [ ] `MlInvestStrategy`
- [ ] **3.3.** Удалить или адаптировать кастомные методы в стратегиях, которые не вписываются в общий контракт
- [ ] **3.4.** Ввести класс `StrategyContext` с полями:
    - [ ] `Long chatId`
    - [ ] `String symbol`
    - [ ] `String timeframe`
    - [ ] `StrategySettings settings`
    - [ ] `CandleProvider candleProvider`
    - [ ] `ExchangeClient exchangeClient`
    - [ ] `OrderService orderService`
- [ ] **3.5.** Обновить реализации стратегий на использование `StrategyContext`, а не разных наборов аргументов

---

# ✅ 4. Orchestrator v4 (StrategyFacade + StrategyRegistry)

- [ ] **4.1.** Привести `StrategyRegistry` к единому подходу:
    - [ ] регистрация стратегий по аннотации `@StrategyBinding(StrategyType.X)`
    - [ ] Map `<StrategyType, TradingStrategy>` как основной реестр
- [ ] **4.2.** Убедиться, что **каждая** стратегия имеет `StrategyType` и `@StrategyBinding`
- [ ] **4.3.** Привести `StrategyFacade` к чёткой API:

    - [ ] `startStrategy(Long chatId, StrategyType type, String symbol)`
    - [ ] `stopStrategy(Long chatId, StrategyType type)`
    - [ ] `StrategyRunInfo getRunInfo(Long chatId, StrategyType type)`
    - [ ] `List<StrategyRunInfo> getActiveStrategies(Long chatId)`

- [ ] **4.4.** Убедиться, что `StrategyFacade` не зависит от Web-контроллеров и UI
- [ ] **4.5.** Переподключить все места, где стратегии запускались напрямую, на использование `StrategyFacade`

---

# ✅ 5. Market & Exchange слой

- [ ] **5.1.** Ввести единый интерфейс `CandleProvider`:
    - [ ] метод `Candle getLatest(String symbol, String timeframe)`
    - [ ] метод `List<Candle> getHistory(String symbol, String timeframe, int limit)`
- [ ] **5.2.** Обновить `SmartFusionCandleService` и другие сервисы свечей так, чтобы они реализовывали `CandleProvider`
- [ ] **5.3.** Заменить во всех стратегиях прямую работу с MarketStream / WebSocket на `CandleProvider`
- [ ] **5.4.** Сгруппировать классы, связанные со стримами/рынком, в пакет `com.chicu.aitradebot.market.*`
    - [ ] WebSocket клиенты
    - [ ] Resampler’ы
    - [ ] Aggregator’ы
- [ ] **5.5.** Нормализовать `ExchangeClientFactory`:
    - [ ] единый метод `get(String exchange, NetworkType networkType)`
    - [ ] обработка ошибок и логирование
    - [ ] тестовый метод для проверки доступности биржи

---

# ✅ 6. API v4 (REST слой)

- [ ] **6.1.** Определить единый REST-контроллер для стратегий: `/api/strategy`
    - [ ] старт / стоп
    - [ ] статус
    - [ ] список активных
- [ ] **6.2.** Определить единый REST-контроллер для графика: `/api/chart`
    - [ ] загрузка истории `/history`
    - [ ] точки сделок `/trades`
- [ ] **6.3.** Удалить или объединить старые/дублирующие API (старые контроллеры, которые дублируют функциональность)
- [ ] **6.4.** Привести Web-контроллеры под такую структуру страниц:
    - [ ] `/dashboard` — общая панель
    - [ ] `/strategies` — список стратегий
    - [ ] `/strategies/{type}/settings` — настройки конкретной стратегии
    - [ ] `/strategies/{type}/dashboard` — дашборд конкретной стратегии (график + метрики)

---

# ✅ 7. Frontend v2 (JS-модули для дашборда)

Разделить функциональность по модулям в `/js/dashboard`:

- [ ] **7.1.** `config.js` — базовые URL, константы:
    - [ ] `API_BASE`
    - [ ] пути `/api/strategy`, `/api/chart`, `/api/smartfusion/settings` и т.д.
- [ ] **7.2.** `chart.js` — отрисовка графика (Lightweight Charts)
- [ ] **7.3.** `ws.js` — WebSocket клиент для live-обновлений
- [ ] **7.4.** `controls.js` — кнопки «запуск/стоп», выбор таймфрейма, экспорт PNG
- [ ] **7.5.** `stats.js` — обновление блоков метрик: PnL, ROI, WinRate, баланс
- [ ] **7.6.** `trades.js` — загрузка и отображение таблицы сделок

Проверка:

- [ ] **7.7.** Все модули подключаются как ES modules (`type="module"`)
- [ ] **7.8.** Нет ненужных глобальных переменных
- [ ] **7.9.** Логика для разных страниц не смешивается (график → только там, где есть контейнер `#candles-chart`)

---

# ✅ 8. UI улучшения (страницы стратегий)

- [ ] **8.1.** В списке стратегий (`/strategies`) добавить кнопку **«Посмотреть»**, ведущую на  
  `/strategies/{type}/dashboard?symbol=...`
- [ ] **8.2.** На `strategy-dashboard.html`:
    - [ ] использовать единый компонент графика из `chart.js`
    - [ ] показывать статус WebSocket (ONLINE/OFFLINE)
    - [ ] отображать текущий символ, timeframe, сеть (MAINNET/TESTNET)
- [ ] **8.3.** Добавить визуальные элементы:
    - [ ] точки сделок (BUY/SELL) на графике
    - [ ] подсветка последней цены
    - [ ] автообновление кратких метрик над графиком

---

# ✅ 9. Очистка и оптимизация

- [ ] **9.1.** После стабилизации — удалить все классы, помеченные `@Deprecated`, которые подтверждённо не используются
- [ ] **9.2.** Оптимизировать `pom.xml`:
    - [ ] убрать неиспользуемые зависимости
    - [ ] убрать дубли библиотек
    - [ ] актуализировать версии критичных libs
- [ ] **9.3.** Проверить и устранить циклические зависимости (между пакетами и модулями)
- [ ] **9.4.** Обновить `README.md`:
    - [ ] кратко описать архитектуру v4
    - [ ] указать слои и основные пакеты
    - [ ] описать, как добавлять новую стратегию
- [ ] **9.5.** Добавить UML/диаграммы (можно PlantUML или рисунок в docs):
    - [ ] диаграмма слоёв
    - [ ] диаграмма взаимодействия Orchestrator ↔ Strategy ↔ Market ↔ Exchange

---

# 🚀 Финализация

- [ ] **F.1.** Провести финальный обзор архитектуры v4 (самостоятельно или с ревью)
- [ ] **F.2.** Зафиксировать состояние в отдельной ветке/релизе (например, `v4-architecture-stable`)
- [ ] **F.3.** Сформировать небольшой `ROADMAP_V5.md`:
    - [ ] улучшение ML/RL слоя
    - [ ] оптимизация производительности
    - [ ] расширение стратегий
- [ ] **F.4.** (опционально) Архивировать старую архитектуру v3 в отдельную ветку `legacy-v3`

---
