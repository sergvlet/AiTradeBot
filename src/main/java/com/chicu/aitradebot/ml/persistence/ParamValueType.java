package com.chicu.aitradebot.ml.persistence;

/**
 * Тип значения параметра в пространстве тюнинга.
 * Важно: это НЕ диапазоны в коде, это только тип интерпретации.
 */
public enum ParamValueType {
    INT,
    DECIMAL,
    BOOLEAN,
    STRING
}
